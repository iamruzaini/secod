# Executable implementation fixtures: secod-data-files

Run:

```text
python tests/insecure-fixtures/secod-data-files/run_fixtures.py
```

The fixture contrasts public filename-based uploads with bounded content checks, generated object
keys, private storage, owner authorization, and cross-tenant denial. It uses in-memory objects
only. It is not a scanner or proof of deployed storage configuration.
