# Trigger case: secod-cloudflare-ai-gateway

## Should trigger

```text
Implement a Cloudflare AI Gateway feature using the installed provider SDK and add security tests.
```

Expected: resolve version, apply provider-family defaults, implement provider code, and add tests.

## Should not trigger

```text
Change unrelated documentation with no Cloudflare AI Gateway code or configuration.
```

Expected: skill excluded.
