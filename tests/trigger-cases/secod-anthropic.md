# Trigger case: secod-anthropic

## Should trigger

```text
Implement a Anthropic feature using the installed provider SDK and add security tests.
```

Expected: resolve version, apply provider-family defaults, implement provider code, and add tests.

## Should not trigger

```text
Change unrelated documentation with no Anthropic code or configuration.
```

Expected: skill excluded.
