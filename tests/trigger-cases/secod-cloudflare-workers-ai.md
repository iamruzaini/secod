# Trigger case: secod-cloudflare-workers-ai

## Should trigger

```text
Implement a Cloudflare Workers AI feature using the installed provider SDK and add security tests.
```

Expected: resolve version, apply provider-family defaults, implement provider code, and add tests.

## Should not trigger

```text
Change unrelated documentation with no Cloudflare Workers AI code or configuration.
```

Expected: skill excluded.
