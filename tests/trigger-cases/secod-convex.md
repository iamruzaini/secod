# Trigger case: secod-convex

## Should trigger

```text
Implement a Convex feature using the installed provider SDK and add security tests.
```

Expected: resolve version, apply provider-family defaults, implement provider code, and add tests.

## Should not trigger

```text
Change unrelated documentation with no Convex code or configuration.
```

Expected: skill excluded.
