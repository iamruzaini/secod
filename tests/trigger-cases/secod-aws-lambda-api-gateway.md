# Trigger case: secod-aws-lambda-api-gateway

## Should trigger

```text
Implement a AWS Lambda and API Gateway feature using the installed provider SDK and add security tests.
```

Expected: resolve version, apply provider-family defaults, implement provider code, and add tests.

## Should not trigger

```text
Change unrelated documentation with no AWS Lambda and API Gateway code or configuration.
```

Expected: skill excluded.
