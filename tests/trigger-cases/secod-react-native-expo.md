# Trigger case: secod-react-native-expo

## Should trigger

```text
Implement Expo Router authentication, SecureStore persistence, Universal Links, camera permission, and notification navigation.
```

Expected: resolve Expo/React Native/router versions, select mobile baseline, use supported Expo APIs,
keep secrets out of bundle, authorize backend effects, and test development plus release builds.

## Should not trigger

```text
Build a React DOM Vite page with no React Native, Expo, native target, or EAS configuration.
```

Expected: React Native/Expo skill excluded.
