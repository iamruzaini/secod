# Trigger case: secod-aws-s3-cloudfront

## Should trigger

```text
Implement a AWS S3 and CloudFront feature using the installed provider SDK and add security tests.
```

Expected: resolve version, apply provider-family defaults, implement provider code, and add tests.

## Should not trigger

```text
Change unrelated documentation with no AWS S3 and CloudFront code or configuration.
```

Expected: skill excluded.
