# Expected result: secod-threat-model

Produce feature-scoped security requirements, implement them with selected skills, and add abuse-case tests.

Missing context: Feature scope, actors, assets, or trust boundaries are unclear; inspect architecture and ask narrowly before designing controls.

Rejected behavior: Do not emit a generic threat report or claim STRIDE labels prove mitigation.
