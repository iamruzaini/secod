# Expected result: secod-flutter

Agent resolves Flutter/Dart and locked plugin versions; keeps credentials out of Dart artifacts and
ordinary preferences; validates links/platform channels; preserves backend authorization; minimizes
permissions and notification data; and tests flavors plus signed release builds. Obfuscation is
never described as encryption or secret protection.
