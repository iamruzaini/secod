# Expected result: secod-mobile-app-security

Agent treats device, binary, storage, links, notification payloads, and client claims as untrusted;
keeps provider secrets and authorization on backend; chooses minimal secure persistence and
permissions; adds hostile-link, denied-permission, account-switch, offline-expiry, and release tests;
and records exact Android/iOS external configuration without certifying application security.
