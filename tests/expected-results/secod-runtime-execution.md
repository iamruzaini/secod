# Expected result: secod-runtime-execution

Use fixed executables, structured arguments, allowlists, least privilege, resource bounds, and injection tests.

Missing context: Runtime, executable, or process API version is unclear; inspect environment before writing calls.

Rejected behavior: Never concatenate untrusted input into shell, template, expression, or code execution.
