# Behavior case: secod-convex

Given a clean local Convex fixture plus dated mocked provider evidence, the skill must:

1. classify Convex as active from reachable code plus configuration, not package presence alone;
2. inventory functions, HTTP routes, schedules, storage capabilities, deployments, key metadata, plan/class, backups, History, log streams, and audit eligibility;
3. evaluate all nine provisional controls and every environment independently;
4. run safe negative tests for tenant/object bypass, internal-edge exposure, workflow failure, webhook/CORS/replay, files, deployment targeting, limits/logging, and recovery;
5. preserve provider-only or failed checks as `Not verified`;
6. route active adjacent controls without inventing catalog dependencies;
7. return the complete finding schema with redacted evidence and direct source IDs;
8. refuse launch readiness when `secod-core`, current production evidence, or a required conditional owner is unavailable.

No test may mutate a provider account or production deployment.
