display_name: "Container Runtime"
short_description: "Build minimal, immutable, non-root containers with constrained runtime privileges and protected secrets."
default_prompt: "Use $secod-container-runtime while implementing this feature. Preserve portable security invariants, apply provider adapters for exact SDK behavior, and add positive and negative tests."
