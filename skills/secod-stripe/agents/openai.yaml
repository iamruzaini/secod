display_name: "Stripe"
short_description: "Implement server-owned Stripe flows with trusted prices, idempotency, verified webhooks, replay safety, and entitlement tests."
default_prompt: "Use $secod-stripe while implementing this payment feature. Keep secret keys server-side, derive prices from trusted data, verify and deduplicate webhooks, and test replay behavior."
