display_name: "AWS Lambda and API Gateway"
short_description: "Implement secure, version-qualified AWS Lambda and API Gateway code and provider-specific tests."
default_prompt: "Use $secod-aws-lambda-api-gateway while implementing this AWS Lambda and API Gateway feature. Resolve exact versions, follow direct official docs, preserve authorization boundaries, and add positive and negative tests."
