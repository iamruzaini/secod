display_name: "Convex"
short_description: "Authenticate and authorize every public Convex function at the server boundary; make privileged, scheduled and ctx.run* implementation paths internal-only; treat storage URLs and…"
default_prompt: "Use $secod-convex to review this project. Preserve missing evidence as Not verified."
