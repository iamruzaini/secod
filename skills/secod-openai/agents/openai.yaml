display_name: "Openai"
short_description: "Implement OpenAI integrations with server-held keys, validated structured output, constrained tools, data minimization, and adversarial tests."
default_prompt: "Use $secod-openai while implementing this OpenAI feature. Resolve the installed SDK, keep keys server-side, validate model output, authorize tools independently, and add adversarial tests."
