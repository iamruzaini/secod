display_name: "Supabase"
short_description: "Implement Supabase RLS, tenant-safe queries, publishable/secret key boundaries, Storage policies, and database-backed authorization tests."
default_prompt: "Use $secod-supabase while implementing this Supabase feature. Design RLS with the schema, keep elevated keys server-only, scope Storage and Realtime access, and add policy tests."
