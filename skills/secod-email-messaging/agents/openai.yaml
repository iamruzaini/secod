display_name: "Email Messaging"
short_description: "Implement recipient-bound, expiring, single-use messaging and recovery flows with abuse controls."
default_prompt: "Use $secod-email-messaging while implementing this feature. Preserve portable security invariants, apply provider adapters for exact SDK behavior, and add positive and negative tests."
