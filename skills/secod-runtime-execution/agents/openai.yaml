display_name: "Runtime Execution"
short_description: "Prevent OS command injection, argument injection and server-side template injection at every server-side execution boundary; avoid shells, use safe process APIs and least-privilege execution contexts."
default_prompt: "Use $secod-runtime-execution to review this project. Preserve missing evidence as Not verified."
