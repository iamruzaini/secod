display_name: "Identity Access"
short_description: "Implement backend authentication, deny-by-default authorization, tenant isolation, secure sessions, and identity-focused tests."
default_prompt: "Use $secod-identity-access while implementing this identity feature. Inspect the existing identity model, enforce authorization at the server boundary, and add positive and negative tests."
