display_name: "Identity Access"
short_description: "Enforce authentication and authorization at the backend; validate every credential, authenticator, session, share link and token lifecycle; deny by default across user, tenant,…"
default_prompt: "Use $secod-identity-access to review this project. Preserve missing evidence as Not verified."
