display_name: "Secrets Config"
short_description: "Implement server-only, scoped, environment-separated secrets and validated secure configuration."
default_prompt: "Use $secod-secrets-config while implementing this feature. Preserve portable security invariants, apply provider adapters for exact SDK behavior, and add positive and negative tests."
