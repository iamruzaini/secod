display_name: "Next.js"
short_description: "Build secure Next.js App or Pages Router features with explicit server/client boundaries, authorization, validation, and scoped caching."
default_prompt: "Use $secod-nextjs while implementing this Next.js feature. Resolve the installed version and router, keep privileged code server-only, authorize every action or route, and add tests."
