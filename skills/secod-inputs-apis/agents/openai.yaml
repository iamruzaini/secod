display_name: "Inputs APIs"
short_description: "Build validated, authorized, bounded API and webhook boundaries that resist injection, SSRF, and unsafe redirects."
default_prompt: "Use $secod-inputs-apis while implementing this API boundary. Validate untrusted input, authorize server-side, constrain outbound requests, and add abuse-case tests."
