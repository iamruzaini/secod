display_name: "Better Auth"
short_description: "Implement secure, version-qualified Better Auth code and provider-specific tests."
default_prompt: "Use $secod-better-auth while implementing this Better Auth feature. Resolve exact versions, follow direct official docs, preserve authorization boundaries, and add positive and negative tests."
