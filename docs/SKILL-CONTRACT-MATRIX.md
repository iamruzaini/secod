# Skill Catalog Matrix

Generated from `catalog.json` by `scripts/sync_catalog.py`. Implementation contracts live in
each skill directory.

| Skill | Layer | Current catalog summary | Dependencies |
|---|---|---|---|
| `secod-core` | General baseline | Implement secure Core behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | None |
| `secod-threat-model` | General baseline | Implement secure Threat Model behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-identity-access` | General baseline | Implement secure Identity Access behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-web-app-security` | General baseline | Implement secure Web App Security behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-inputs-apis` | General baseline | Implement secure Inputs APIs behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-runtime-execution` | General baseline | Implement secure Runtime Execution behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-crypto-data-protection` | General baseline | Implement secure Crypto Data Protection behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-data-files` | General baseline | Implement secure Data Files behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-abuse-limits` | General baseline | Implement secure Abuse Limits behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-secrets-config` | General baseline | Implement secure Secrets Config behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-packages-delivery` | General baseline | Implement secure Packages Delivery behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-vulnerability-management` | General baseline | Implement secure Vulnerability Management behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-observability-response` | General baseline | Implement secure Observability Response behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-payments-billing` | General baseline | Implement secure Payments Billing behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-ai-api-integrations` | General baseline | Implement secure AI API Integrations behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-container-runtime` | General baseline | Implement secure Container Runtime behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-email-messaging` | General baseline | Implement secure Email Messaging behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-failure-safety` | General baseline | Implement secure Failure Safety behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-ship-check` | Task completion | Check only current feature, selected-skill invariants, tests added and executed, accidentally introduced secrets, and external configuration still required. | secod-core |
| `secod-supabase-auth` | Framework / provider adapter | Implement secure Supabase Auth behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-identity-access, secod-auth-provider-integrations, secod-supabase |
| `secod-auth0` | Framework / provider adapter | Implement secure Auth0 behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-auth-provider-integrations, secod-identity-access |
| `secod-workos` | Framework / provider adapter | Implement secure Workos behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-auth-provider-integrations, secod-identity-access |
| `secod-better-auth` | Framework / provider adapter | Implement secure Better Auth behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-auth-provider-integrations, secod-identity-access |
| `secod-nextjs` | Framework / provider adapter | Implement secure Next.js behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-mobile-app-security` | Mobile baseline | Implement secure mobile trust boundaries, device storage, verified links, minimal permissions, safe notifications, local persistence, and release builds. | secod-core, secod-identity-access, secod-data-files, secod-secrets-config |
| `secod-react-native-expo` | Mobile framework adapter | Implement secure React Native and Expo storage, links, permissions, notifications, provider SDK boundaries, and production build behavior with supported APIs. | secod-core, secod-mobile-app-security |
| `secod-flutter` | Mobile framework adapter | Implement secure Flutter storage, links, permissions, notifications, provider plugin boundaries, flavors, and signed release builds with supported APIs. | secod-core, secod-mobile-app-security |
| `secod-vercel-platform` | Framework / provider adapter | Implement secure Vercel Platform behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-auth-provider-integrations` | Framework / provider adapter | Implement secure Auth Provider Integrations behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-identity-access |
| `secod-clerk` | Framework / provider adapter | Implement secure Clerk behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-auth-provider-integrations, secod-identity-access |
| `secod-cloudflare` | Framework / provider adapter | Implement secure Cloudflare behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-cloudflare-workers` | Framework / provider adapter | Implement secure Cloudflare Workers behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-cloudflare, secod-identity-access, secod-inputs-apis, secod-secrets-config, secod-abuse-limits, secod-data-files, secod-observability-response |
| `secod-cloudflare-pages` | Framework / provider adapter | Implement secure Cloudflare Pages behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-cloudflare, secod-packages-delivery, secod-secrets-config, secod-web-app-security |
| `secod-cloudflare-queues` | Framework / provider adapter | Implement secure Cloudflare Queues behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-cloudflare, secod-inputs-apis, secod-abuse-limits, secod-observability-response |
| `secod-cloudflare-workflows` | Framework / provider adapter | Implement secure Cloudflare Workflows behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-cloudflare, secod-inputs-apis, secod-abuse-limits, secod-observability-response |
| `secod-cloudflare-hyperdrive` | Framework / provider adapter | Implement secure Cloudflare Hyperdrive behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-cloudflare-workers, secod-secrets-config, secod-inputs-apis, secod-data-files |
| `secod-cloudflare-vectorize` | Framework / provider adapter | Implement secure Cloudflare Vectorize behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-cloudflare-workers, secod-ai-api-integrations, secod-data-files, secod-observability-response |
| `secod-cloudflare-workers-ai` | Framework / provider adapter | Implement secure Cloudflare Workers AI behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-cloudflare-workers, secod-ai-api-integrations, secod-abuse-limits, secod-observability-response |
| `secod-cloudflare-ai-gateway` | Framework / provider adapter | Implement secure Cloudflare AI Gateway behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-cloudflare, secod-ai-api-integrations, secod-secrets-config, secod-abuse-limits, secod-observability-response |
| `secod-supabase` | Framework / provider adapter | Implement secure Supabase behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-firebase` | Framework / provider adapter | Implement secure Firebase behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-identity-access, secod-secrets-config, secod-abuse-limits, secod-observability-response |
| `secod-neon` | Framework / provider adapter | Implement secure Neon behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-identity-access |
| `secod-convex` | Framework / provider adapter | Implement secure Convex behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-aws-web` | Framework / provider adapter | Implement secure AWS Web behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-aws-lambda-api-gateway` | Framework / provider adapter | Implement secure AWS Lambda API Gateway behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-aws-web, secod-identity-access, secod-inputs-apis, secod-abuse-limits, secod-secrets-config |
| `secod-aws-cognito` | Framework / provider adapter | Implement secure AWS Cognito behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-aws-web, secod-identity-access, secod-auth-provider-integrations |
| `secod-aws-s3-cloudfront` | Framework / provider adapter | Implement secure AWS S3 Cloudfront behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-aws-web, secod-data-files, secod-secrets-config, secod-abuse-limits |
| `secod-aws-data-services` | Framework / provider adapter | Implement secure AWS Data Services behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-aws-web, secod-data-files, secod-inputs-apis, secod-secrets-config, secod-observability-response |
| `secod-google-cloud-web` | Framework / provider adapter | Implement secure Google Cloud Web behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core |
| `secod-google-cloud-storage` | Framework / provider adapter | Implement secure Google Cloud Storage behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-google-cloud-web, secod-data-files, secod-secrets-config, secod-abuse-limits |
| `secod-stripe` | Framework / provider adapter | Implement secure Stripe behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-payments-billing |
| `secod-polar` | Framework / provider adapter | Implement secure Polar behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-payments-billing |
| `secod-lemonsqueezy` | Framework / provider adapter | Implement secure Lemonsqueezy behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-payments-billing |
| `secod-dodo-payments` | Framework / provider adapter | Implement secure Dodo Payments behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-payments-billing |
| `secod-whop` | Framework / provider adapter | Implement secure Whop behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-payments-billing |
| `secod-openai` | Framework / provider adapter | Implement secure Openai behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-ai-api-integrations |
| `secod-anthropic` | Framework / provider adapter | Implement secure Anthropic behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-ai-api-integrations |
| `secod-google-genai` | Framework / provider adapter | Implement secure Google Genai behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-ai-api-integrations |
| `secod-xai-grok` | Framework / provider adapter | Implement secure Xai Grok behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-ai-api-integrations |
| `secod-vercel-ai` | Framework / provider adapter | Implement secure Vercel AI behavior with explicit trust boundaries, supported APIs, official documentation, and positive and negative tests. | secod-core, secod-ai-api-integrations |
